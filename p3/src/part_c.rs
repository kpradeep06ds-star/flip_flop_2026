// use fancy_regex::Regex;
// use std::{cmp::max};
use crate::part_b::{self, one_digit_7, one_each, red_blue_green};
// find a 7 and don't find any other number
pub fn part_c(lines: String) -> i32{
    2
}